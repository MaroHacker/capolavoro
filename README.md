# Usage

### Dependencies

- `nodejs` and `npm`

```bash
# unzip dell'archivio
$ unrar -x capolavoro.rar

cd capolavoro

# installo le dependencies
npm install

# avvio il sito
npm run dev -- --open
```
